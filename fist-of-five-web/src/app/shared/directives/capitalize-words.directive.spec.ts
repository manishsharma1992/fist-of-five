import { CapitalizeWordsDirective } from './capitalize-words.directive';

describe('CapitalizeWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new CapitalizeWordsDirective();
    expect(directive).toBeTruthy();
  });
});
